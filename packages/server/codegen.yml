overwrite: true
schema: "http://localhost:4000"
documents: null
generates:
  ../../types/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-mongodb"
  ./graphql.schema.json:
    plugins:
      - "introspection"
